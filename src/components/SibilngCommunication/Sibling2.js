import React from "react";

const Sibing2 = (props) => {
  return (
    <>
      {/* <h1>{props.value}</h1> */}
      <button onClick={props.handleClick}>Click</button>
    </>
  );
};

export default Sibing2;
